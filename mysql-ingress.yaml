apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mysql-ingress
  
spec:
  rules:
  - host: test.com
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: mysql
            port:
              number: 3306
