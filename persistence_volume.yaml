apiVersion: v1
kind: PersistentVolume
metadata:
  name: ec-auth-mysql-pv-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 20Gi
  accessModes:
    - ReadOnlyMany
  hostPath:
    path: "/mnt/data"
